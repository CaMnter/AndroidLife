<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.camnter.content.provider.plugin.plugin">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <provider
            android:name="com.camnter.content.provider.plugin.plugin.PluginContentProvider"
            android:authorities="com.camnter.content.provider.plugin.plugin.PluginContentProvider"
            android:process=":plugin_content_provider"
            android:exported="true"/>

    </application>

</manifest>
